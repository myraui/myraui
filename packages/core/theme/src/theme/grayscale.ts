import { ThemedThemeTokens } from '../theme.types';

export type ThemeGrayscale = 'disabled';

export const grayscale: ThemedThemeTokens<'grayscale'> = {
  light: {
    disabled: '80%',
  },
  dark: {
    disabled: '80%',
  },
};
