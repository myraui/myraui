import { ColorPalette } from '../types';
import { ColorMode } from '@myra-ui/colors';

export const colorPalette: Record<ColorMode, ColorPalette> = {
  dark: {
    neutral: 'gray',
    action: 'blue',
    foreground: 'gray',
  },
  light: {
    neutral: 'gray',
    action: 'blue',
    foreground: 'gray',
  },
};
