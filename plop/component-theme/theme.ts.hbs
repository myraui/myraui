import { VariantProps } from 'tailwind-variants';
import { tv } from '../utils/tv';

const {{packageName}} = tv({
   base: '',
   variants: {
     // Add your variants here
   }
});


export type {{capitalize packageName}}VariantProps = VariantProps<typeof {{packageName}}>;

export { {{packageName}} };