{
  "name": "@myraui/{{packageName}}",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "{{destination}}/src",
  "projectType": "library",
  "tags": ["scope:common"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "{{rootDir}}/{{destination}}/dist",
        "rootDir": "{{rootDir}}/{{destination}}/src",
        "main": "{{rootDir}}/{{destination}}/src/index.ts",
        "tsConfig": "{{rootDir}}/{{destination}}/tsconfig.lib.json",
        "assets": []
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "{{rootDir}}/{{destination}}/jest.config.ts"
      }
    }
  }
}
