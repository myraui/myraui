import {renderHook} from "@testing-library/react-hooks";

import { {{camelCase packageName}} } from "../src";

describe("{{camelCase packageName}}", () => {
  it("should work correctly", () => {
    const {result} = renderHook(() => {{camelCase packageName}}());

    // Add your test here
  });
});
