import {forwardRef} from "@myraui/system";
import { Use{{capitalize packageName}}Props, use{{capitalize packageName}} } from "./use-{{packageName}}";
import React from 'react';

export interface {{capitalize packageName}}Props extends Use{{capitalize packageName}}Props {}

const {{capitalize packageName}} = forwardRef<"div", {{capitalize packageName}}Props>((props, ref) => {
  const {Component, domRef, children, styles, ...otherProps} =  use{{capitalize packageName}}({...props, ref});

  return (
  <Component ref={domRef} className={styles} {...otherProps}>
    {children}
  </Component>
  );
});

{{capitalize packageName}}.displayName = "MyraUI.{{capitalize packageName}}";

export default {{capitalize packageName}};
